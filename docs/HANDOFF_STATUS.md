# Статус перехода на ролевые интерфейсы — хендофф

Этот файл — снимок состояния, чтобы продолжить работу в новом чате без повторного прохождения
всей истории. Пишется по просьбе пользователя из-за переполнения контекста текущей сессии.
Первая версия — снимок на 2026-08-14 (см. ниже), но с тех пор файл вёлся дальше в
хронологическом порядке. **Если контекст снова переполнился — начинайте с этого раздела**,
он актуальнее и приоритетнее заголовка "снимок на 2026-08-14" ниже.

---

## СНИМОК НА 2026-08-20, КОНЕЦ ДНЯ — начинать здесь

За сегодня одной длинной сессией сделан весь P0 и весь P1 из дорожной карты (артефакт
«Дорожная карта Airsoft Economy»,
https://claude.ai/code/artifact/41e25809-3dde-41b2-828e-66eb373b3518 — ссылка не хранится
нигде больше в репозитории, сохраните её, если работаете с планом дальше). Все коммиты
закоммичены И запушены на `master`, рабочее дерево чистое (`git status` пустой на конец сессии).

### Что сделано (в хронологическом порядке коммитов)
1. `a2b7d2b` — монитор миграций, экран Админ → Миграции (`024_migrations_status.sql`).
2. `26db111` — статус «отклонено» для заявок на игру + архивация проекта с заморозкой экономики
   (`025_reject_game_participant.sql`, `026_project_archival.sql`).
3. `7c34e74` — схема под мультиклубность, **сознательно не подключена к RLS**
   (`027_clubs_schema.sql`) — `clubs`/`club_admins` существуют, но RLS без единой политики,
   недоступны никому.
4. `e152f27` — клубы стали заполняемыми из админки, без изоляции данных
   (`028_clubs_admin_ui.sql`, новый `components/AdminClubsTab.tsx`).
5. `56c9cfc` — архивация и безопасное (с очисткой Storage) удаление клуба
   (`029_club_archival.sql`).
6. `35d7c7b` — каскад видимости для закрытых проектов на уровень стороны/участников/заданий/
   транзакций + видимость «кто уже в игре» для организатора
   (`030_closed_project_cascade.sql`, `031_active_game_visibility.sql`).
7. `017cb05` — внутриприложенная лента уведомлений, рабочий колокольчик
   (`032_notifications.sql`, новый `components/NotificationsScreen.tsx`).
8. `cdc68fd` — повтор при сетевых сбоях для регистрации и действий с заданиями (НЕ для денежных
   переводов — не защищены от повторного выполнения, см. `lib/retry.ts`).
9. `2bc7657` — **починил реальный баг, найденный пользователем вживую на self-hosted**:
   `active_game_id` (031) писался на сервер только в момент самого нажатия «Приступить к игре»
   — если игрок был в этом режиме ещё до редеплоя (или просто перезапустил вкладку), сервер
   никогда не узнавал, и бейдж «🟢 в игре» у организатора не появлялся никогда. Теперь
   восстановление активной игры из AsyncStorage на маунте тоже дописывает `active_game_id`.
   **Сам фикс НЕ переподтверждён вживую после деплоя** — только исправлен и запушен.

### Единственное, что реально проверено вживую
Self-hosted (`api.district-airsoft.ru`) — пользователь лично проверил вживую: все 5 карточек
Админа задеплоились (Клубы/Миграции в том числе), список участников игры не пустой (баг был
только в отсутствии зелёного бейджа, не в самом списке) — это и навело на находку выше. Плюс
ранее в этой же сессии — облачный тестовый проект (`kmijlaxjxipmauhuofph.supabase.co`) через
случайно найденную сессию в браузере: экран Миграций работает безупречно, участники/заявки игры
«Штурм высоты» не падают, корректно деградируют там, где на сервере ещё нет нужных колонок/
таблиц. Найдены и починены три реальных бага итого (устаревший `lib/migrations.ts`, неверный код
ошибки PostgREST `PGRST205` вместо `42P01`, и `active_game_id`-синхронизация выше). **Всё
остальное — notifications-триггеры, RPC-guards архивации, retry-обёртка — НЕ проверено вживую.**

### Первое, что нужно сделать в следующей сессии
**Облачный тестовый проект отстаёт на 8 миграций** (025–032, подтверждено напрямую через REST
API) — если он ещё используется, применить их через SQL Editor одним заходом. Self-hosted эту
проблему не имеет — таймер подхватывает пуши сам, подтверждено пользователем вживую.

### Что дальше (открытое решение, не мой выбор)
Пользователю предложено на выбор: (а) P2 из дорожной карты — публичный каталог игр + прокат
снаряжения, зависит от того, что клубы уже в схеме, или (б) доделать самый рискованный
недоделанный кусок P0 — подключить `clubs`/`club_admins` к RLS и заменить единый глобальный
`is_admin()` на клубную роль. Это самая рискованная задача во всём плане (трогает практически
каждую политику в схеме одновременно) — начинать её стоит отдельной, осторожной сессией, не с
разгону.

### Другие открытые хвосты (не блокирующие, см. подробности ниже по файлу)
- `lib/migrations.ts` нужно обновлять вручную при каждой новой миграции — уже один раз забыли
  (025–032), проверяйте эту синхронизацию отдельно, не доверяя только экрану Миграций.
- Денежные переводы (`SendMoneyModal.tsx`, депозиты, списание торговца) остаются без защиты от
  повторного выполнения при сетевом сбое — нужен механизм идемпотентности, если понадобится
  распространить retry и на них.
- Полноценная офлайн-очередь действий (не просто повтор при кратковременном обрыве) осталась
  несделанной — сознательно, это отдельная по объёму задача.

---

## Как читать остальную часть файла (снимок на 2026-08-14 и далее)
1. «Готово» — реализовано и закоммичено, можно проверять в приложении.
2. «Открытая проблема» — активно диагностировали, **не решили**, начинать отсюда.
3. «Не начато» — по плану, но кода ещё нет.

---

## Готово (в репозитории)

Коммиты на `master` (ещё не запушены на момент записи — см. ниже):
- `78809b1` — Этап 1 (оболочка + переключатель ролей) и Этап 2 (организатор: главная/игры/мастер создания)
- `908316f` — временное debug-логирование в `CreateTaskModal.tsx` (см. «Открытая проблема» ниже)

Плюс более ранние коммиты этой же кампании (UX/UI-аудит, до перехода на ролевые интерфейсы):
`1bca964`, `70e6d3e`, `3b7c359`, `741b9bd` — accessibility, safe area, TabBar, pull-to-refresh
и т.п. Это отдельный, уже полностью закрытый пласт работы.

### Этап 1 — Оболочка + переключатель ролей
Файл: `components/Dashboard.tsx` (переписан полностью).
- Логотип/название в шапке (`⚔️ Airsoft Economy`), раньше отсутствовали.
- Селектор проекта грузит теперь **все** проекты, а не только с `economy_enabled` —
  экономика по-прежнему гейтится отдельно через `economyProjectId`
  (см. `Dashboard.tsx`, вычисляется как `activeProject?.economy_enabled ? activeProjectId : null`)
  и передаётся в `WalletScreen`/`TeamsScreen`/`TeamCommanderScreen`/`AdminScreen`.
- Переключатель ролей: `RoleKey = 'admin' | 'organizer' | 'sideCommander' | 'teamCommander' | 'trader' | 'player'`.
  Эксклюзивный — виден только если ролей >1 (компонент `TabBar` переиспользован).
  Приоритет по умолчанию: админ → организатор → командующий стороной → командир команды → торговец → игрок.
  Источник — `useCapabilities()` + `profile.role`, без новых полей в БД.
- Пустой слот под уведомления в шапке (иконка `bell-outline`, без логики — фичи ещё нет).

### Этап 2 — Организатор: главная + список игр + мастер создания
Файлы: `components/OrganizerScreen.tsx` (переписан), `components/CreateGameWizard.tsx` (новый).
- `OrganizerScreen` принимает пропс `view: 'overview' | 'games'` — под-навигация (Обзор/Игры/
  Экономика проекта) теперь плоская, на уровне `Dashboard.tsx` (`organizerTab`), а не вложенная.
- Обзор: карточки «Заявок на рассмотрении» / «Подтверждено участников» (агрегируется по
  `game_participants.status` для всех доступных организатору игр), быстрый переход к созданию
  игры и к ближайшей игре.
- Игры: плоский список (без захода через проект), у каждой — проект/полигон/дата/статус
  (вычисляется по датам)/кол-во сторон/участников.
- `CreateGameWizard` — 5 шагов (Основная информация / Дата и полигон / Стороны / Командующие
  сторонами / Проверка и сохранение). Стороны редактируемые (не жёстко «Белые/Красные»),
  назначение командующего — сразу в мастере. Список проектов для мастера — только те, где
  профиль есть в `project_organizers` (проверено запросом к этой таблице, не через
  `useCapabilities`).
- Создание игры через админку (`AdminProjectsTab`'s «Новая игра») **пока не убрано** —
  оставлено как резервный путь, как и планировалось (снос — задача Этапа 8, после того как
  новый путь обкатан).

---

## РЕШЕНО — создание задания падало с RLS (см. supabase/016_fix_task_returning_rls.sql)

Настоящая причина найдена и исправлена 2026-08-14 (заняло ~2 дня): `read tasks`
политика использовала `can_view_task(id)`, которая **сама** делает `select ...
from tasks where id = ...`. PostgREST всегда шлёт `INSERT ... RETURNING *`, а
Postgres проверяет SELECT-политику на только что вставленной строке **в
рамках той же INSERT-команды**. По MVCC-правилам команда не видит собственные
ещё не завершённые изменения через отдельный скан той же таблицы — значит
`exists(...)` внутри `can_view_task` всегда находил ноль строк и политика
всегда возвращала `false`, независимо от роли/пользователя. Без `RETURNING`
всё работало (доп. проверки просто не было) — это и сбивало с толку на
протяжении всего расследования (политики/функции/токены/секреты — всё было
идентично и корректно, поэтому все инфраструктурные гипотезы проверялись и
отваливались одна за другой).

Подтверждено экспериментально (как postgres, без RLS вообще):
`with ins as (insert ... returning id) select exists(select 1 from tasks
where id = ins.id) from ins` → `false`.

Фикс: `supabase/016_fix_task_returning_rls.sql` — новая функция
`can_view_task_row(...)`, принимающая колонки строки как аргументы вместо
повторного запроса к `tasks`; `read tasks` политика переключена на неё.
`can_view_task(uuid)` не тронута — она по-прежнему корректна для
`task_attachments` (там нет self-scan проблемы, т.к. запрос к `tasks` идёт
из отдельной, уже закоммиченной строки в отдельном запросе).

Применено и подтверждено рабочим на self-hosted (`api.district-airsoft.ru`).
**На облачном тестовом проекте (`kmijlaxjxipmauhuofph.supabase.co`) миграция
016 пока не применена** — нужно вставить файл целиком в SQL Editor, если
тестовый проект ещё используется.

Debug-логи в `CreateTaskModal.tsx` убраны (были нужны только для этого
расследования).

### Историческая заметка (закрыто, оставлено для контекста)

**Симптом:** в `CreateTaskModal` (внутри `GameManageScreen` → «Задания» → `TasksSection`)
при нажатии «Создать» — ошибка `new row violates row-level security policy for table "tasks"`.

**Важно:** это НЕ баг, внесённый переходом на ролевые интерфейсы — политика и функции RLS
не менялись в рамках Этапов 1–2. Обнаружено попутно при тестировании администратором.

### Что уже проверено и подтверждено рабочим
- `profiles.role = 'admin'` для аккаунта `admin@airsoft-economy.local`
  (id `38a50fc2-69ee-4dcb-b86a-fe91e13005fc`) — подтверждено прямым SQL-запросом.
- `auth.uid()` в реальном запросе приложения **совпадает** с этим id (подтверждено через
  временный `console.log` в `CreateTaskModal.tsx`, см. ниже) — сессия корректна, это не
  протухший токен и не путаница аккаунтов.
- Функции `public.is_admin()` и `public.is_game_organizer(uuid)` в базе **дословно совпадают**
  с `supabase/002_roles_and_games.sql` / `supabase/007_scoped_roles.sql` — расхождений нет.
- При симуляции JWT через `set_config('request.jwt.claims', ...)` + `set local role authenticated`
  (SQL Editor по умолчанию выполняет как `postgres`, что обходит RLS и даёт `auth.uid() = NULL`,
  поэтому обычный запрос там ничего не показывает про реальную сессию — только так и через
  Impersonate с явно выбранным пользователем можно получить осмысленный ответ):
  - `is_admin()` → `true`
  - `is_game_organizer(game_id)` → `true` для **обеих** существующих игр
    («Воскреска 27 07 26» = `3fd20307-4c89-4613-b1e2-622c39687fe2`,
    «Штурм высоты» = `806d40b7-2e93-44d2-ae8d-274ea5ef8e49`).
- **Найдено и исправлено** реальное расхождение: политика `"participants create tasks" on tasks`
  в базе была СТАРОЙ версией (требовала членства в `game_participants`, без варианта
  `is_game_organizer`/`is_game_trader_for_side`) — файл `013_tasks.sql` в репозитории содержит
  правильную версию, но задеплоена она не была. Пересоздана вручную через SQL Editor
  (`drop policy` → `create policy`, дословно из `013_tasks.sql`), подтверждена через
  `pg_policies` — новый `with_check` виден в базе.
- **Несмотря на это — ошибка сохраняется** даже после подтверждённого фикса политики.
  Последний известный статус: не переспросили точный текст свежей ошибки после этого фикса
  (пользователь исчерпал терпение к пошаговой SQL-диагностике — см. ниже).

### Что НЕ проверено — начинать отсюда
1. **Точный текст ошибки после фикса политики** — не подтверждён. Возможно, ошибка уже другая
   (не RLS), и время потрачено на не ту гипотезу.
2. Не проверено содержимое (а не факт существования) политики `commander or self registers
   participant` / `organizer updates participants` и триггера `check_task()` — теоретически
   может быть ещё одна политика/триггер, блокирующая независимо от `is_game_organizer`.
3. Временный `console.log('DEBUG create task', {...})` в `components/CreateTaskModal.tsx`
   (в функции `submit()`, прямо перед `.insert()`) — **оставлен специально**, выводит
   `authUid`, `customerProfileId`, `gameId`, `sideId`. Использовать через DevTools → Console
   при следующей попытке. **Убрать после решения проблемы.**

### Рекомендация для следующего чата
Не повторять пошаговую SQL-переписку — она заняла огромное количество сообщений и вымотала
пользователя (дословно: «ты заебал»). Вместо этого:
- Либо получить временный доступ с сервисной ролью / прямым подключением к Postgres, чтобы
  диагностировать самостоятельно, без ретрансляции через пользователя.
- Либо запросить у пользователя **один** дамп: `pg_policies` + определения всех триггеров на
  `tasks` (`check_task`) одним запросом, целиком, без обрезания колонок (например, через
  `Export → CSV` в Supabase Studio, а не через таблицу результатов, которая обрезает текст).

---

## ИСПРАВЛЕНО — миграция 010 применена на self-hosted (подтверждено 2026-08-20)

**Обновление 2026-08-20:** пользователь подтвердил в чате, что на self-hosted
(`api.district-airsoft.ru`) на данный момент применены все актуальные миграции, включая 010 —
без отдельной живой проверки со стороны Клода в рамках этой сессии (нет доступа к БД напрямую).
Раз появился «Мониторинг миграций» (см. раздел ниже, экран Админ → Миграции) — дальнейшие такие
вопросы можно закрывать через него, а не через ручной SQL-аудит. Ниже — исходное описание бага,
оставлено для истории.

Аудит всех 15 файлов миграций (`supabase/*.sql`) против реальной базы показал: файл
`supabase/010_game_details.sql` целиком не задеплоен (таблицы `game_stages`, `game_attachments`
не существуют, как и их политики + storage bucket `game-attachments`). Из-за этого разделы
«Этапы» и «Файлы игры» в `GameManageScreen` молча показывают пустоту (запрос к
несуществующей таблице проглатывается как `data ?? []`).

Также не задеплоена саморегистрация из этого же файла (политики `commander or self registers
participant` / `commander or self unregisters participant`).

**Фикс был готов, теперь выполнен:** содержимое `supabase/010_game_details.sql`
целиком безопасно перевыполнить (`create table if not exists`, `drop policy if exists` перед
create) — просто вставить файл как есть в SQL Editor и запустить.

Внимание: в файле есть `create policy` без предварительного `drop if exists` для
`game_stages`/`game_attachments` (строки после `create table` для этих двух таблиц) — это
безопасно ТОЛЬКО потому что сами таблицы ещё не существуют, значит и политик на них быть не
может. Если это будет перевыполняться повторно после первого успешного прогона — тогда уже
нужно добавить `drop policy if exists` вручную, иначе будет «already exists», как уже было с
`tasks`.

Замечание в сторону: раз одна миграция (010) и одна политика (013) разошлись с репозиторием,
**весь остальной список миграций стоит считать подозрительным**, пока не аудирован явно.
Готовый аудит-скрипт — `migration_audit.sql`, отправлен пользователю файлом в чате ранее
(не в репозитории; при необходимости пересобрать: `grep` по `create (or replace )?function`,
`create policy`, `create table if not exists` во всех `supabase/*.sql`).

---

## Не начато (по исходному плану)

Полный план с деталями (меню/главные страницы/риски по каждой роли) — в артефакте,
опубликованном ранее в этом чате пользователю (ссылка есть у пользователя, не сохранена
в репозитории). Кратко, порядок и зависимости:

- ~~**Этап 3 — Заявки (организатор) + Регистрация команды (командир)**~~ — **готово.**
  `GameManageScreen` теперь вкладочный (Обзор/Стороны/Участники/Заявки/Задания), «Заявки»
  показывает «Новые»/«Одобренные» (без «Отклонённые» — статус `rejected` в
  `game_participants.status` по-прежнему сознательно отложен, «отклонить» = удалить
  pending-строку). `TeamCommanderScreen`'s регистрация состава уже была в основном готова —
  добавлено `Alert`-подтверждение при снятии уже подтверждённого игрока, чтобы командир не
  снял его случайным тапом. Миграций не потребовалось — все нужные RLS-политики на
  `game_participants` уже были в базе (см. ниже).
- ~~**Этап 4 — Игрок.**~~ — **готово.** 4 вкладки (Главная/Игры/Моя команда/Деньги),
  `PlayerHomeScreen.tsx` (бывший `ParticipantScreen.tsx`, урезан до мини-лендинга),
  новые `PlayerGamesScreen.tsx` (плоский список игр с 4 состояниями: Предстоит/
  Прошла/На рассмотрении/Доступна для записи, кросс-проектный) и
  `PlayerTeamScreen.tsx` (read-only ростер+баланс). `GameCardScreen.tsx` избавлен
  от командирской логики (дубль `TeamCommanderScreen` из Этапа 3), добавлена
  самостоятельная отмена заявки (RLS уже была, `010_game_details.sql`) и «Состав
  стороны»+«Задания» перенесены из `BriefingScreen.tsx`, который удалён.
  Миграций не потребовалось.
- ~~**Этап 5 — Командир команды: остальное.**~~ — **готово.** `TeamCommanderScreen.tsx`
  разбит на вкладки Моя команда/Игры/Бюджет (тот же паттерн Chip-subNav, что и в
  `GameManageScreen` из Этапа 3) — чисто рендер-рефакторинг, `AmountForm` переехал
  из ростерных карточек в «Бюджет», загрузка/мутации не менялись. Миграций не
  потребовалось.
- ~~**Этап 6 — Торговец.**~~ — **готово.** `supabase/017_trader_charge.sql`: RPC
  `trader_charge_participant`/`trader_charge_team` (деньги уходят на баланс самого
  торговца, сторона цели проверяется на сервере через `is_game_trader_for_side`,
  не берётся параметром от клиента), новый вид `trader_charge`. Новый
  `components/TraderChargeSection.tsx` (Участник/Команда, `AmountForm` на
  каждого) вмонтирован в `TraderScreen.tsx`. Попутно почин `valid_source`/
  `valid_destination` — они не обновлялись с 005 и блокировали `task_reward`
  (см. технический долг ниже, было незамечено весь этот проект).
- ~~**Этап 7 — Командующий стороной.**~~ — **готово.** `SideCommanderScreen.tsx`
  разбит на вкладки Моя сторона/Команды/Задания. «Команды» — новый плоский
  список без доп. запросов (та же уже загруженная `teams`, другой рендер).
  «Задания» — `TasksSection` с новым пропом `commandedSideIds` (по образцу
  `traderSideIds` у торговца, прокинут и в `TaskDetailSheet` для `canAssign`).
  Миграций не потребовалось — видимость команд/заданий на своей стороне уже
  была на RLS через `commands_side_for_team_game`.
- ~~**Этап 8 — Админ.**~~ — **готово.** `AdminScreen.tsx` — главная с 3 карточками
  (Пользователи/Команды/Проекты и игры), «Проекты» получил под-переключатель
  Проекты/Полигоны. Создание игры убрано из `AdminProjectsTab` — админ уже
  безусловно организатор на уровне RLS (`is_admin() or ...` в
  `is_project_organizer`/`is_game_organizer`), не хватало только клиентского
  обхода в `OrganizerScreen.fetchOrganizerProjects` (по образцу уже
  существующего обхода в `fetchGames` того же файла) — без литерального
  `insert` в `project_organizers`. Открытую саморегистрацию для всех
  пользователей сознательно не делали (уточнено с пользователем — только
  админ). Пополнение баланса убрано из `AdminUsersTab`/`AdminTeamsTab`
  (баланс остался как просмотр), переехало в `TeamsScreen.tsx`
  («Экономика проекта» у организатора) — новая вкладка «Пополнить» с
  переключателем Команда/Участник, поиск участника по номеру/позывному по
  образцу Этапа 5. Миграций не потребовалось — оба RPC
  (`deposit_to_team`/`deposit_to_participant`) уже существовали.
- **Этап 9 — Хвосты.** Частично сделано (см. ниже — справка/обучение). Удаление
  старых элементов, если остались; уведомления остаются пустым слотом до появления
  фичи.

---

## После хендоффа 2026-08-14 (не отражено выше)

Коммиты поверх Этапа 8, в хронологическом порядке:

- `d82c169` — **Справка и обучение в приложении.** Иконка «?» в шапке (`Dashboard.tsx`,
  рядом с пустым `bell-outline`-слотом) открывает `HelpScreen.tsx` — полноэкранный
  текстовый гид по всем 6 ролям (как получить роль, вкладки, типичные сценарии,
  предупреждения) + глоссарий. Контент — `lib/helpContent.ts`. При первом заходе
  в роль автоматически показывается `OnboardingCarousel.tsx` (2–4 слайда, «Пропустить»,
  повтор из справки) — контент в `lib/onboardingContent.ts`, флаг «уже показывали»
  на пару пользователь+роль в `lib/onboardingStorage.ts` (AsyncStorage). `RoleKey`/
  `ROLE_META` вынесены из `Dashboard.tsx` в `lib/roles.ts` (общие для справки/карусели
  и дашборда). Миграций не потребовалось. Тот же контент опубликован пользователю как
  отдельный веб-гайд (артефакт, ссылка у пользователя, не хранится в репозитории) —
  при следующих функциональных изменениях его тоже нужно обновлять вручную, синхронизации
  с `lib/helpContent.ts` нет.
- `22c5e87` — блок регистрации (галочка с правилами, выбор стороны, «Зарегистрироваться»/
  «Отменить заявку») убран с `PlayerHomeScreen` (там теперь только просмотр ближайшей
  подтверждённой игры), остался только в `PlayerGamesScreen` → `GameCardScreen`. Новый
  проп `GameCardScreen.showRegistration`.
- `a93a308` — списки игр/сторон в большинстве ролевых вкладок (Главная и Игры игрока,
  Обзор/Игры организатора, Игры командира команды, Моя сторона/Команды командующего
  стороной, список игр торговца) теперь фильтруются по `activeProjectId` — раньше
  показывали игры/стороны сразу по всем проектам. `useCapabilities` обогащает
  `commandedSides`/`traderGames` полем `project_id` через join на `games`, чтобы экраны
  фильтровали без лишних запросов. Побочный эффект: `SideCommanderScreen` и
  `TraderScreen` получили новые пустые состояния («У вас нет сторон в этом проекте» /
  «Нет игр в этом проекте»), отличные от «вас вообще никуда не назначили».
- `5374bc1` — новая фича «активная игра». На `PlayerHomeScreen` у ближайшей игры
  появилась зелёная кнопка «Приступить к игре» (`lib/activeGameStorage.ts` — состояние
  `{gameId, projectId}` в AsyncStorage на пользователя). Пока активна: `PlayerHomeScreen`
  всегда рендерит `GameCardScreen` этой игры (без кнопки «Назад», вместо неё красная
  «Окончить игру» с подтверждением через `confirmAsync`), а переключатель проекта в
  шапке (`Dashboard.tsx`) блокируется на её `project_id` во всех ролях (`Chip` получил
  проп `disabled`). Состояние переживает перезапуск приложения — восстанавливается в
  `Dashboard` по `profile.id` при монтировании.

Ни один из этих четырёх коммитов не подтверждён вживую в браузере в рамках диалога —
проверялось только `npx tsc --noEmit` (чисто) и, для d82c169, что `expo start --web`
собирается без ошибок бандлера. Логина в приложение с реальным аккаунтом в рамках
этих сессий не было (self-hosted бэкенд `api.district-airsoft.ru`, тестового аккаунта
нет под рукой).

- **«Закрытые проекты»** (см. `git log` на `master` за 2026-08-18 для хэша коммита). Галка
  `is_closed` у проекта (`supabase/018_closed_projects.sql`) + две новые таблицы
  `project_allowed_teams`/`project_allowed_players`. Закрытый проект (и его игры —
  политика `read games` тоже переписана) невидим всем, кроме админа, организаторов
  проекта/игры и явно добавленных команд/игроков. Галку ставит только админ
  (`AdminProjectsTab`, проекты остаются admin-only write, как в 007); список
  доступа редактируют и админ, и организаторы проекта — общий компонент
  `ClosedProjectAccessSheet.tsx`, вызывается из `AdminProjectsTab` и из
  `OrganizerScreen` (Обзор, для активного проекта, если он закрыт).
  Сознательно НЕ каскадировали закрытие глубже (`game_sides`, `game_participants`,
  `tasks`, `game_stages`, транзакции и т.п.) — до них нельзя добраться из UI, не
  видя сам список игр, а трогать RLS больше необходимого не хотелось (см. историю
  выше). Если позже понадобится полная изоляция данных — это открытый хвост.
  Обе новые политики `read projects`/`read games` написаны с оглядкой на баг
  `can_view_task` (см. РЕШЕНО выше): `read projects` читает только собственные
  колонки строки (`is_closed`, `id`), новые функции `project_is_closed()`/
  `can_view_project()`, которые сами делают `select ... from projects`, вызываются
  только из политики `games` (другая таблица — self-scan не грозит), но никогда
  из политики самого `projects`.

  **Применено и подтверждено вживую 2026-08-18 на облачном тестовом проекте**
  (`kmijlaxjxipmauhuofph.supabase.co`) — после применения миграции первая попытка
  падала с `Could not find the table ... in the schema cache` даже после
  `NOTIFY pgrst, 'reload schema'`; ушло само в течение сессии (видимо, отложенный
  реload). Проверено вручную в браузере под живым логином: создание проекта сразу
  с галкой → окно доступа открывается автоматически; добавление/удаление команды и
  игрока; переоткрытие окна показывает сохранённое состояние; та же
  `ClosedProjectAccessSheet` работает одинаково с вкладки «Админ» и с вкладки
  «Организатор»; **отрицательный кейс подтверждён пользователем лично** — участник
  команды, не добавленной в список (Bravo), не видит закрытый проект в
  переключателе, а участник добавленной команды (Alpha, включая её командира) —
  видит, как и положено.

  **НЕ применено на self-hosted/проде** (`api.district-airsoft.ru`) — только на
  облачном тестовом проекте. `.env` сейчас указывает на облачный тестовый проект
  (было переключено в рамках этой сессии специально для тестирования; self-hosted
  закомментирован ниже в файле) — **не забыть переключить обратно на self-hosted
  перед обычной работой**, если только следующая сессия не продолжает тестировать
  здесь же.

- **Мониторинг миграций (2026-08-20, закоммичено и запушено — `a2b7d2b`).** Новая вкладка
  Админ → «Миграции» (`components/AdminMigrationsTab.tsx`) визуально сверяет
  список файлов `supabase/*.sql` с журналом их применения на текущем сервере —
  зелёная галка/красный крестик и дата по каждому файлу.

  Список ожидаемых файлов жёстко прописан в `lib/migrations.ts` (клиент не
  может читать репозиторий во время работы) — **при каждой новой миграции его
  нужно дополнять вручную**, иначе экран её просто не покажет.

  Журнал — не новая идея: таблицу `public._migrations_applied` уже создаёт и
  ведёт `deploy/supabase/migrate.sh` на self-hosted при каждом прогоне
  `airsoft-migrate.timer` (раз в 5 минут). Новая миграция
  `supabase/024_migrations_status.sql` добавила к ней RLS (админ читает;
  админ может вставить/удалить строку) и создаёт ту же таблицу там, где её
  ещё нет — то есть на облачном тестовом проекте, у которого нет таймера и
  который всегда обновлялся вручную через SQL Editor. Для таких случаев в
  экране есть кнопка «Отметить вручную» — она не запускает миграцию, только
  фиксирует в журнале факт, что админ применил файл сам. Отдельно экран
  показывает записи журнала без соответствующего файла в `lib/migrations.ts`
  (например, если файл переименовали).

  Это отвечает на пункт «Новое, из этой сессии» ниже в разделе техдолга — но
  только на вопрос «применён ли файл вообще»; сверки СОДЕРЖИМОГО уже
  применённых файлов с репозиторием (как задумывался `migration_audit.sql`)
  это не делает.

  Новые/изменённые файлы: `supabase/024_migrations_status.sql`,
  `lib/migrations.ts`, `components/AdminMigrationsTab.tsx`, плюс правки в
  `lib/database.types.ts`, `components/AdminScreen.tsx`, `lib/helpContent.ts`.
  Проверено `npx tsc --noEmit` (чисто) и что `expo start --web` собирается и
  показывает экран входа без ошибок бандлера — живого логина под админом на
  экране Админ → Миграции конкретно (просмотр самого списка/галок в
  интерфейсе) в рамках диалога не было.

  **Обновление 2026-08-20 (после пуша):** пользователь подтвердил, что на
  self-hosted (`api.district-airsoft.ru`) сейчас применены все актуальные
  миграции — то есть `024_migrations_status.sql` тоже подхвачен таймером.
  Это НЕ проверялось Клодом напрямую через сам экран «Миграции» в рамках
  этой сессии (нет тестового аккаунта), так что сам UI экрана по-прежнему
  не подтверждён вживую — только то, что данные, которые он должен
  показывать, теперь корректны на self-hosted.

  **Всё ещё открыто:**
  1. Облачный тестовый проект (`kmijlaxjxipmauhuofph.supabase.co`) — статус
     не упоминался в последнем подтверждении; как обычно, нужно вставить
     `024_migrations_status.sql` в SQL Editor вручную, если проект ещё
     используется.
  2. Живая проверка самого экрана Админ → Миграции (не только данных,
     которые он читает) — открыть его под админом хотя бы на одном сервере
     и посмотреть, что он действительно рисует галки/кресты корректно.

---

## Технический долг / открытые вопросы (из фазы 0, ещё актуальны)
- ~~Статус «отклонено» для заявок — отложен, нужна миграция схемы `game_participants.status`~~ —
  сделано 2026-08-20, см. «P0: статус «отклонено»» ниже.
- ~~Отмена заявки игроком — нужна RLS-политика~~ — политика уже есть (`010_game_details.sql`,
  применена и подтверждена рабочей), нужен только UI на стороне игрока (см. Этап 4).
- ~~Списание торговца — новый RPC + новый вид операции~~ — сделано, см. Этап 6.
- ~~`personal_transactions.valid_source`/`valid_destination` не пускали
  `task_reward`~~ — исправлено в `017_trader_charge.sql` заодно с Этапом 6;
  до этой миграции `complete_task` с ненулевой наградой должен был падать на
  constraint'е (не было замечено раньше — награду за задание в этой сессии
  до конца не тестировали).
- **Новое, из этой сессии:** обнаружено расхождение файлов миграций с реальной БД (минимум
  два подтверждённых случая — `013` политика и `010` целиком). Прежде чем полагаться на
  чтение `supabase/*.sql` как на источник истины для НОВЫХ RLS-политик в Этапах 3/4/6 —
  стоит сначала прогнать `migration_audit.sql`-подход по остальным файлам, а не только по 010/013.
  **Обновление 2026-08-20:** теперь есть встроенный визуальный монитор для вопроса «применён ли
  файл вообще» — см. «Мониторинг миграций» выше. Он не заменяет сверку содержимого
  (`migration_audit.sql`-подход), только видимость применения по факту наличия файла в журнале.

---

## Начат P0 из дорожной карты (2026-08-20, ЕЩЁ НЕ ЗАКОММИЧЕНО)

По просьбе пользователя начали делать пункты P0 из опубликованного ранее артефакта-роадмапа
(«Дорожная карта Airsoft Economy»). Сделаны два независимых, низкорисковых пункта; пункты
про таблицу `clubs`/`club_admins`/RLS-аудит (самые рискованные, затрагивают буквально каждую
политику в схеме) сознательно НЕ начаты — см. отдельное сообщение пользователю в чате с
предложением делать их отдельным, более осторожным заходом.

### P0: статус «отклонено» для заявок на игру

`supabase/025_reject_game_participant.sql` — расширил `game_participants.status` до
`'pending' | 'confirmed' | 'rejected'` (был только `pending`/`confirmed`). Организатор теперь
жмёт «Отклонить» (было «Удалить», через тот же delete) — ставит `status = 'rejected'`, строка не
удаляется. Никакой новой RLS-политики не потребовалось: существующая `organizer updates
participants` (007) не ограничивает, в какое значение организатор меняет `status`, а
самостоятельная повторная заявка после отказа сделана как delete-затем-insert на клиенте
(переиспользует существующие `commander or self unregisters participant` / `commander or self
registers participant`), а не через новую UPDATE-политику для реактивации.

Задело 6 файлов помимо миграции — оказалось больше мест, чем казалось на старте, потому что
почти везде, где раньше `game_participants` без статуса просто не существовала строки (удалили
= пропала), теперь строка `rejected` остаётся и должна быть корректно проигнорирована:
- `lib/database.types.ts` — `GameParticipantStatus` расширен.
- `components/GameManageScreen.tsx` — новая `rejectParticipant()` (UPDATE вместо DELETE),
  `removeParticipant()` оставлена как есть (для кика уже подтверждённого участника — это
  осталось хардделитом, статус `rejected` не для этого случая). Кнопка в `ParticipantRowView`
  разветвлена: pending → «Подтвердить»+«Отклонить», иначе → «Удалить».
- `components/GameCardScreen.tsx` — `alreadyRegistered` был `!!myStatus` (при `rejected` игрок
  навсегда завис бы в состоянии «заявка на рассмотрении», без возможности подать заявку заново);
  стал явной проверкой `pending`/`confirmed`. `register()` теперь чистит старую `rejected`-строку
  (delete) перед новым insert — иначе уникальный индекс `(game_id, profile_id)` бы блокировал
  повторную заявку. Роспись состава стороны (`participantsRes` в `load()`) — добавлен
  `.neq('status', 'rejected')`, иначе отклонённый чужой игрок мог всплыть в «Составе стороны».
- `components/TeamCommanderScreen.tsx` — `toggleParticipant()` для одиночного тапа по составу:
  раньше требовалось два тапа на отклонённом игроке (первый тихо чистил старую строку, второй
  реально добавлял); теперь чистит и добавляет за один тап. `pickSide()` (смена стороны команды)
  уже и так делал delete-затем-insert для всех, кого видел в карте — рескан не понадобился.
- `components/SideCommanderScreen.tsx` — ростер стороны собирался без учёта статуса вообще;
  добавлен пропуск `status === 'rejected'`, иначе показывало бы «На подтверждении» (бинарный
  тернарник без варианта на `rejected`).
- `components/TasksSection.tsx` — список участников для назначения заданий не фильтровал по
  статусу вообще (уже включал `pending` — это было и раньше, не трогал); добавил
  `.neq('status', 'rejected')`, чтобы отклонённый не всплывал как «участник» для назначения задач.
- `components/OrganizerScreen.tsx` — счётчик «Участников: N» на карточке игры считал все строки
  без разбора; теперь пропускает `rejected`, иначе счётчик рос бы после каждого отказа.
- `lib/helpContent.ts` — обновлён текст сценария организатора под новую кнопку.

### P0: архивация проекта и заморозка его экономики

`supabase/026_project_archival.sql` — новая `projects.archived_at timestamptz` (null = активен),
плюс `public.project_is_archived(p_project_id)` (та же форма, что уже существующая
`project_economy_enabled`). Добавил одинаковый guard `if public.project_is_archived(...) then
raise exception...` во ВСЕ денежные RPC сразу после уже существующей проверки
`project_economy_enabled`: `transfer_funds`, `deposit_to_team`, `deposit_to_participant`,
`distribute_to_participant`, `transfer_to_team`, `transfer_to_participant`,
`trader_charge_participant`, `trader_charge_team`, и в `complete_task` — только внутри ветки
начисления награды (`v_task.reward is not null and v_task.reward > 0`), сама отметка задания
выполненным не блокируется. Тела функций скопированы как есть из последних версий (008, 014,
017) — менялась только вставка guard'а, никакой другой логики.

Баланс НЕ обнуляется архивацией — это осознанно, снимок остаётся навсегда. Чтение (баланс,
история операций) не блокируется нигде — только новые операции.

Никакой новой RLS-политики не потребовалось: `archived_at` — это просто ещё одна колонка на
`projects`, а `projects` и так admin-only на запись (`admins update projects`, 007) — ту же
политику использует и `is_closed`. **Важно:** роадмап-артефакт (не в репозитории) на момент
написания говорит «admin + организаторы проекта могут переключать» для архивации — это
разошлось с тем, что реально в БД (organizer никогда не имел UPDATE на `projects`, только
на allow-list таблицы для closed). Сделал admin-only, как и всё остальное на этой таблице;
артефакт нужно поправить, если ещё не поправлен.

UI: `components/AdminProjectsTab.tsx` — кнопка «Архивировать»/«Вернуть из архива» в карточке
проекта (не в форме редактирования — отдельное быстрое действие, не завязанное на
Сохранить/Отмена), бейдж 🗄️ в списке проектов. `components/Dashboard.tsx` — тот же бейдж 🗄️
перед названием в переключателе проекта в шапке; **сознательно НЕ стал скрывать архивные
проекты из переключателя** — это отдельное UX-решение (прятать/не прятать), не запрошенное явно,
трогать не стал.

### Общее по обоим пунктам

Изменения: `supabase/025_reject_game_participant.sql`, `supabase/026_project_archival.sql`,
`lib/database.types.ts`, `components/GameManageScreen.tsx`, `components/GameCardScreen.tsx`,
`components/TeamCommanderScreen.tsx`, `components/SideCommanderScreen.tsx`,
`components/TasksSection.tsx`, `components/OrganizerScreen.tsx`, `components/AdminProjectsTab.tsx`,
`components/Dashboard.tsx`, `lib/helpContent.ts`. **Ещё не закоммичено.**

Проверено только `npx tsc --noEmit` (чисто, после каждого крупного шага) и что `expo start --web`
собирается и показывает экран входа без ошибок бандлера/консоли. **Живого логина не было ни разу**
(нет тестового аккаунта) — ни новая кнопка «Отклонить», ни архивация проекта, ни то, что RPC
реально отдают `this project is archived` при вызове из приложения, вживую не проверялись.
Особенно стоит проверить руками после деплоя: полный цикл отказ → «Заявка снова доступна для
записи» → повторная подача (это самая переплетённая с RLS/уникальным индексом часть).

**Закоммичено и запушено `26db111`.**

---

## P0: схема под клубы — только схема, БЕЗ подключения к RLS (2026-08-20, запушено `7c34e74`)

`supabase/027_clubs_schema.sql` — по явному запросу пользователя сделали только «тёмный запуск»
схемы, не трогая ни одной существующей политики/функции:
- Новая `clubs` (id/name/description/created_by/created_at) — RLS включена, политик нет вообще,
  значит таблица недоступна вообще никому (даже `is_admin()`), пока политики не добавят.
- Новая `club_admins` (club_id, profile_id, unique) по образцу `project_organizers` — та же
  история, RLS включена, политик нет.
- `projects.club_id` и `polygons.club_id` — nullable, `references clubs(id) on delete cascade`.
  У всех существующих строк будет `null`, ни на что не влияет.

Сознательно НЕ сделано в этом шаге (следующий, более рискованный заход):
- Никакой RLS-политики на `clubs`/`club_admins`.
- `is_admin()` не тронут — ни одна существующая политика в схеме не меняется.
- `teams`/`profiles` — без `club_id`, специально (см. роадмап-артефакт, причина —
  игроки/команды играют в нескольких клубах).
- Никакого бэкфилла `club_id` для существующих проектов/полигонов — бэкфиллить пока не во что
  (нет ни одной строки в `clubs`).

Ноль изменений в клиентском коде (не тронут `lib/database.types.ts` — типы для этих таблиц
осознанно не добавлены, раз ни у кого нет прав их читать). `npx tsc --noEmit` соответственно
прошёл автоматически (диффа в TS/TSX нет).

**Следующий, отдельный шаг (не начат):** замена `is_admin()` на клубную роль + платформенного
супер-админа, аудит на утечку между клубами, удаление/архивация клуба. Это самый рискованный
кусок всего роадмапа — трогает практически каждую политику в схеме одновременно, начинать стоит
отдельной сессией с особой осторожностью (см. рекомендацию про self-scan баги выше).

---

## P0: клубы стали заполняемыми — минимальный кусок, БЕЗ изоляции данных (2026-08-20, запушено `e152f27`)

По просьбе пользователя (спросил «через какое меню мне создавать и работать с клубами») сделали
следующий маленький, низкорисковый шаг поверх «тёмного запуска» из `027_clubs_schema.sql`:

`supabase/028_clubs_admin_ui.sql` — RLS-политики ТОЛЬКО на `clubs`/`club_admins`, один в один по
образцу уже существующих `project_organizers`: чтение открыто всем авторизованным, запись —
только `is_admin()`. Никакая существующая политика не менялась. `projects.club_id`/
`polygons.club_id` новой политики не потребовали — `projects`/`polygons` и так admin-only на
запись (007/006), так что установка `club_id` уже покрыта.

UI:
- Новый `components/AdminClubsTab.tsx` — список клубов, создание (название/описание),
  карточка клуба с редактированием, удалением и назначением `club_admins` через чипы (тот же
  паттерн, что `project_organizers` в `AdminProjectsTab`). Явно подписано в интерфейсе, что
  `club_admins` пока ни на что не влияет — просто список на будущее.
- Новая вкладка «Клубы» в `AdminScreen.tsx` (5-я карточка на главной админки).
- `AdminProjectsTab.tsx` и `PolygonsScreen.tsx` — чип-пикер «Клуб» в формах создания/
  редактирования проекта и полигона, плюс отображение названия клуба в детальном виде.

**Явно НЕ сделано** — реальной изоляции данных между клубами всё ещё нет: `club_id` нигде не
проверяется в RLS, любой человек с ролью «Админ» по-прежнему видит и правит всё на всём
приложении целиком, `club_admins` не даёт никаких особых прав. Это по-прежнему тот же отдельный
рискованный шаг (замена `is_admin()`), что и раньше.

Изменения: `supabase/028_clubs_admin_ui.sql`, `lib/database.types.ts` (типы `Club`/`ClubAdmin`,
`club_id` у `Project`/`Polygon`), `components/AdminClubsTab.tsx` (новый), `components/AdminScreen.tsx`,
`components/AdminProjectsTab.tsx`, `components/PolygonsScreen.tsx`. `npx tsc --noEmit` чист,
веб-бандл собирается без ошибок. Живого логина не было — создание клуба, назначение
`club_admins` и выбор клуба у проекта/полигона вживую не проверялись.

---

## P0: архивация и удаление клуба (2026-08-20, запушено `56c9cfc`)

По прямому запросу пользователя («архивация и удаление клуба когда сделаем?») доделал пункт 08
из дорожной карты, поверх уже существующих `clubs`/`club_admins` (027/028).

`supabase/029_club_archival.sql` — `clubs.archived_at`, без новых RLS-политик (та же
`admins update clubs` из 028 уже покрывает). Заметно, что архивация клуба **сознательно не
каскадирует** в `archived_at` у его проектов — это отдельное, явное действие админа, не
автоматика (по аналогии с тем, как `is_closed`/`archived_at` у проектов уже держали
разделёнными).

Обнаружил по пути: `on delete cascade` на `projects.club_id`/`polygons.club_id` — из
`027_clubs_schema.sql` — уже физически работал, а кнопка «Удалить клуб» в `AdminClubsTab.tsx`
уже существовала (из прошлого шага) и до этой правки удаляла клуб одним нажатием без всякого
подтверждения — то есть каскадное удаление проектов/полигонов уже было технически возможно
раньше, чем планировалось по дорожной карте. Практический риск был низким (пока ни у одного
клуба нет привязанных проектов/полигонов), но стоило закрыть до того, как кто-то начнёт
реально привязывать данные к клубам.

Теперь `deleteClub()` в `AdminClubsTab.tsx`:
1. Собирает `project_id`/`polygon_id` этого клуба, через них — `game_id`/`task_id`, и по ним —
   все `storage_path` из `game_attachments`/`task_attachments`/`polygon_maps`.
2. Чистит соответствующие бакеты (`game-attachments`/`task-attachments`/`polygon-maps`) —
   Postgres-каскад до Supabase Storage не дотягивается, файлы бы утекали безвозвратно иначе.
3. Только потом удаляет саму строку клуба — дальше каскад в БД доделывает всё остальное сам
   (игры, заявки, задания, транзакции — всё, что уже висело на `project_id`/`polygon_id`).

Подтверждение — ввод названия клуба текстом (как удаление репозитория на GitHub), кнопка
неактивна, пока текст не совпадёт дословно. Перед этим показывается, сколько именно проектов и
полигонов будет удалено (`count: 'exact', head: true` запрос) — чтобы не удалить не глядя.
Команды и игроки клуба, как и раньше, не задеты — у них по-прежнему нет `club_id`.

Изменения: `supabase/029_club_archival.sql`, `lib/database.types.ts` (`archived_at` у `Club`),
`components/AdminClubsTab.tsx`. `npx tsc --noEmit` чист, веб-бандл собирается. Живого логина не
было — весь флоу удаления (сбор storage_path → чистка бакетов → удаление строки → каскад) ни
разу не прогонялся на реальных данных, только прочитан и собран по аналогии с уже рабочим
удалением полигона в `PolygonsScreen.tsx`. Стоит явно проверить руками на клубе с реально
привязанными проектом/полигоном с вложениями, прежде чем полагаться на эту кнопку в бою.

---

## Начат P1 из дорожной карты (2026-08-20)

Пользователь: уведомления сначала как внутриприложенная лента (без push/expo-notifications/EAS),
офлайн-устойчивость — не откладывать.

### P1: каскад видимости для закрытых проектов — `supabase/030_closed_project_cascade.sql`

`018_closed_projects.sql` в своё время сознательно остановился на `projects`/`games` — сторон,
участников, заданий, этапов, вложений, транзакций это не касалось, и часть из них (`game_sides`,
`game_team_sides`, `game_stages`, `game_attachments`, `game_traders`, `game_trader_sides`,
`transactions`) обнаружились с политикой `using (true)` — то есть их читал вообще любой
авторизованный пользователь, знай он `game_id`/`task_id`, независимо от закрытости проекта. Новая
`public.can_view_game(p_game_id)` (по образцу `can_view_project`, безопасна к self-scan ровно
потому что запрашивает `games` — другую таблицу, не ту, чью политику расширяет) добавлена через
`and` к существующим политикам всех этих таблиц плюс `tasks`/`task_attachments`/
`personal_transactions` — тела политик не переписывались, только сужены. Для проекта, который не
закрыт, `can_view_project()`/`can_view_game()` безусловно `true` — на все обычные (не закрытые)
проекты это не влияет вообще. Правок в коде приложения не потребовалось. Не проверено вживую.

### P1: видимость «кто уже в игре» для организатора — `supabase/031_active_game_visibility.sql`

«Активная игра» была чисто локальным AsyncStorage-флагом на устройстве
(`lib/activeGameStorage.ts`) — организатор никак не мог увидеть, кто уже нажал «Приступить к
игре». Добавлена `profiles.active_game_id` (текущее состояние, не история чекинов — этого
достаточно для того, что просили). Новых RLS-политик не потребовалось: `read profiles` уже
`using (true)`, `update own profile or admin` уже разрешает менять любую колонку своей строки.

`components/Dashboard.tsx` — `startGame`/`endGame` теперь параллельно с локальным
AsyncStorage-состоянием (оно осталось источником истины для восстановления при перезапуске
приложения на этом устройстве — офлайн-дружелюбно) best-effort пишут `active_game_id` на сервер,
без ожидания и без всплывающих ошибок — это видимость, не критичная функциональность.
`components/GameManageScreen.tsx` — запрос участников теперь тянет `active_game_id`, у
подтверждённых/на рассмотрении участников появляется бейдж «🟢 в игре», когда
`profile.active_game_id === game.id`.

### P1: уведомления (внутриприложенная лента) — `supabase/032_notifications.sql`

Решено с пользователем: сначала внутриприложенная лента, без push/expo-notifications/EAS — это
отдельный, гораздо больший трек. Новая `notifications` — заполняется НЕ через правки каждого RPC,
а тремя триггерами на уже существующих таблицах (`tasks`, `game_participants`,
`personal_transactions`) — срабатывают независимо от того, какой именно RPC/код-путь изменил
строку, так что новый способ создать деньги/назначить задание в будущем не потребует помнить
добавить уведомление отдельно. Покрыто: назначение задания (`claim_task`/`assign_task`, с
подавлением уведомления себе при самостоятельном взятии), заявка подтверждена/отклонена, деньги
получены лично (депозит/перевод/награда за задание/списание торговцем — торговец получатель).
Сознательно НЕ покрыто: `transactions` (команда-команде) — нет одного естественного личного
получателя без похода за капитаном команды, отложено. Никакой новой INSERT-политики для клиентов
нет вообще — триггеры security definer, обходят RLS на запись, значит подделать чужое уведомление
через `.insert()` с клиента невозможно в принципе (политики INSERT просто не существует).

`components/NotificationsScreen.tsx` (новый, по образцу `HelpScreen.tsx`) + рабочий колокольчик в
`Dashboard.tsx` (был пустой `View`, стал `Pressable` со счётчиком непрочитанных). Открытие ленты
помечает всё прочитанным, но в рамках этого же просмотра непрочитанные на момент открытия всё
ещё визуально выделены.

### Живая проверка сессией (2026-08-20) — впервые за весь день

В браузер-вкладке случайно обнаружилась настоящая залогиненная сессия на облачном тестовом
проекте (`kmijlaxjxipmauhuofph.supabase.co`, роль — админ+все роли на аккаунте). Воспользовался,
чтобы наконец проверить хоть что-то вживую в этой сессии:

- **Admin → Миграции реально работает** — открыл, увидел живые данные. Но обнаружил реальный баг
  по пути: `lib/migrations.ts` не обновлялся с миграции 024, хотя после неё добавилось ещё 8 файлов
  (025–032) — экран врал «24 из 24» вместо «24 из 32». Исправлено.
- **`NotificationsScreen`/`AdminMigrationsTab` детектировали отсутствие таблицы неправильно** —
  ждали Postgres-код `42P01`, а PostgREST на самом деле отдаёт `PGRST205` («Could not find the
  table ... in the schema cache») — подтверждено прямым запросом к REST API облачного проекта.
  `NotificationsScreen` из-за этого не показывал дружелюбное сообщение (falls back на сырой текст
  ошибки — не критично, но некрасиво), `AdminMigrationsTab` эту ветку в живую вообще не проверял
  (журнал миграций там уже существовал, до 42P01/PGRST205 дело не дошло). Исправлено в обоих.
- Прогнал вручную: Админ → Клубы/Проекты (ожидаемо 404/400 — 027-032 на облаке не применены),
  Организатор → игра «Штурм высоты» → Участники/Заявки — ничего не падает, просто тихо показывает
  пусто там, где запрос тянет ещё не существующую на этом сервере колонку (`active_game_id`) —
  деградация без крэша, самоисправится после применения миграций. Обзор организатора (счётчики
  «Заявок на рассмотрении»/«Подтверждено участников» из P0 item 04) считает верно.
- **Вывод: облачный тестовый проект отстаёт на миграции 025–032** — если он всё ещё используется,
  нужно применить их через SQL Editor одним махом (весь диапазон одним заходом, как раньше).
  Self-hosted не проверялся в рамках этой сессии.

### P1: устойчивость к слабой связи — `lib/retry.ts`

Сузил формулировку из роадмапа сознательно: это про кратковременные обрывы (запрос не прошёл,
повтор через секунду успешен), а не про полный офлайн с очередью действий и синхронизацией потом
— второе на порядок больше по объёму (персистентность между перезапусками, разрешение конфликтов,
UI статуса синхронизации) и не то, что здесь строилось. Без новых зависимостей (нет
`@react-native-community/netinfo` в package.json) — `withRetry()` не проверяет состояние сети
заранее, просто повторяет запрос, если он упал с сетевой на вид ошибкой (`network`/`fetch`/
`timeout`/`ECONNRESET` и т.п. в тексте), с экспоненциальной задержкой, до 3 повторов.

**Важно про безопасность повтора — не везде это безопасно.** Обёрнуто только там, где повторный
вызов физически не может продублировать эффект:
- `GameCardScreen.register()` — `insert` в `game_participants`, защищён уникальным индексом
  `(game_id, profile_id)`: если первая попытка на самом деле прошла, повтор просто упадёт на
  дубликате, а не создаст вторую заявку.
- `TaskDetailSheet.tsx`'s `run()` — покрывает `claim_task`/`assign_task`/`complete_task`/
  `cancel_task` разом (все идут через один и тот же helper): каждый из этих RPC сам проверяет
  статус задания и падает с «уже взято»/«уже выполнено» на повторный вызов после реального успеха.

**Сознательно НЕ обёрнуто: `transfer_to_team`/`transfer_to_participant` (`SendMoneyModal.tsx`),
депозиты, списание торговца.** Все денежные RPC ничем не защищены от повторного исполнения — если
первый вызов реально прошёл на сервере, а ответ потерялся из-за обрыва, слепой повтор отправил бы
деньги второй раз. Чтобы сделать переводы retry-safe, нужен отдельный механизм (идемпотентный
ключ от клиента + проверка на сервере перед списанием) — это отдельная, более крупная задача,
которую сознательно не стал делать походя вместе с остальным.
